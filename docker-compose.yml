services:
  api:
    build:
      context: ./backend
    ports:
      - "5039:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
  web:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
